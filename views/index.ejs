<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="d-flex flex-row justify-content-between">
            <div class="d-flex flex-column align-items-center emailTextBlock">
                <textarea class="form-control" id="emailTextInput" placeholder="Введите текст письма..." rows="10"></textarea>

                <form method="POST" action="/download_files">
                  <button type="submit"  class="btn btn-secondary" id="btnTextUpload">Загрузить файлы</button>
                </form>
                
              <form method="POST" action="/start">
                <button type="submit" class="btn btn-primary" id="btnRun">Запустить рассылку</button>
              </form>


            </div>
            <div class="d-flex flex-row emailAddressBlock">
                <div class="d-flex flex-column align-items-center emailAddressTableBlock">
                    <table class="table table-bordered table-sm" id="emailAddressTable">
                        <thead>
                          <tr>
                            <th scope="col">#</th>
                            <th scope="col">Email-адрес</th>
                          </tr>
                        </thead>
                        <tbody>
                          <% for (var n=0; n<(data.length); n++) {%>
                          <tr>
                            <th scope="row"><%=n%></th>
                            <td><%=data[n].email%></td>
                          </tr>
                          <% } %>
                          


                          
                       
                        </tbody>
                      </table>

                          <form method="POST" action="http://localhost:3001/download_email_address" enctype="multipart/form-data">
                          <div class="form-group">
                            <label for="exampleFormControlFile1">Загрузить адреса из файла</label>
                            <input name="filedata" accept=".xlsx, .xls" type="file" class="form-control-file" id="exampleFormControlFile1" >
                            <br>
                            <button type="submit" class="btn btn-primary" id="btnRefreshTable">Загрузить таблицу</button>
                          </div>
                        </form>
                      

                      <form method="GET" action="/refresh_table">
                        <button type="submit" class="btn btn-primary" id="btnRefreshTable">Обновить таблицу</button>
                      </form>

                </div>
                <div class="d-flex flex-column emailAdressInputBlock">
                  <form method="POST" action="/add_email">
                    <input type="email" class="form-control" id="emailAddressInput" placeholder="Введите email-адрес..." required>
                    <button type="submit" class="btn btn-primary" id="btnAddressAdd">Добавить адрес в таблицу</button>
                  </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>